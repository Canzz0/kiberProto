syntax = "proto3";

package chat.v1;

service ChatService {
  rpc Guess (GuessRequest) returns (GuessResponse);
}

message GuessRequest {
  string departmanlar = 1;
  string konumlar = 2;
  string turler = 3;
  string altturler = 4;
  string chatHistory = 5;
  string guid = 6;
  string category = 7;
  string message = 8;
  // production=false iken gateway'in gönderdiği prompt/tool override paketi
  // (JSON string). Boş ise chat-bot kod içindeki varsayılanları kullanır.
  string aiOverrides = 9;
    string checkin = 11;
  string checkout = 12;
}

// Nested message structure reflecting the described apiResponse JSON
message ApiResponse {
  string description = 1;
  string language = 2;
  int32 department = 3;
  int32 location = 4;
  int32 altturler = 5;
  int32 turler = 6;
  int32 category = 7;
  string messageType = 8;
  string response = 9;
  int32 roomNumber = 10;
  string checkin = 11;
  string checkout = 12;
}

message GuessResponse {
  // The envelope object "message" can be represented by the root GuessResponse itself.
  ApiResponse apiResponse = 1;
  int32 totalTokens = 2;
  int32 inputTokens = 3;
  int32 outputTokens = 4;
}
